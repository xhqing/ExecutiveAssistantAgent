# 运行时数据（Agent 产物：产品成品、日志、复盘、config 等）——本地业务数据，非开源内容
# 其中可能含明文账号密码 / token，一并忽略
docs/
artifacts/

# node_modules
node_modules/

# 系统杂物
.DS_Store
__pycache__/
*.pyc

# Claude Code 个人本地配置（机器本地，不提交）
.claude/settings.local.json

# hook 运行时状态文件（机器本地）
.claude/.memory-changed-this-session
.claude/.last-memory-checkpoint

# 临时产物（见 .claude/rules/tmp-dir-for-artifacts.md）
tmp/


# 本地敏感变更记录（个人隐私类变更，不公开）
CHANGELOG.local.md

# 本地敏感待办（个人隐私类，不公开）
TODO.local.md
TODO-archive.local.md

# 飞书备份目录（目录下所有文件即要备份到飞书的私有文件，2026-09-01 用户立目录制——
# 只忽略目录一行，不写具体文件名：.gitignore 是公开文件，文件名即信息）
backup/

# 本机密钥通用兜底（保留 .env.example）
.env
.env.*
!.env.example
